# Environment / secrets
.env
# service account keys
*.json

# Python
__pycache__/
*.py[cod]
.venv/

# Streamlit
.streamlit/secrets.toml
secrets.toml

# Model outputs / results (may contain business data)
results/*
# Exception: pre-baked synthetic Bayesian results (no real data, needed for Streamlit Cloud)
!results/mmm_model_results.csv
!results/mmm_fitted_values.csv
!results/mmm_contrib_hdi.csv

# Notebooks with potentially sensitive outputs
# fit_model.ipynb — safe to track (synthetic data only, no real outputs)
